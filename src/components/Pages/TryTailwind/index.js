import { useEffect, useRef, useState, createContext } from 'react';
import { makeStyles } from '@mui/styles';
import Nav from '../../Molecule/Nav';
const useStyles = makeStyles((theme) => {

})

const TryTailwind = () => {
  const classes = useStyles()
 
  return (
    <div >
      <Nav />

      <div className='' >
          sdafsd
      </div>
    </div>
  );
}

export default TryTailwind